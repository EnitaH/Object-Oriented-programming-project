package Design;

import java.util.Scanner;

public interface RoomBehavior {
    boolean startRoom(Player player, Scanner scanner);  // Method to start the room and execute its logic
    //void solveChallenge(Player player); // Method to solve challenges in the room
}
